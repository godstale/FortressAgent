# CLAUDE.md — Claude Code 전용 보충 지침

> **먼저 [`AGENTS.md`](./AGENTS.md)를 읽으십시오.** 기술 스택, 코딩 컨벤션, 빌드/테스트 명령, Git 규칙, 멀티 에이전트 협업 규칙, 안전 원칙 등 핵심 지침은 모두 그곳에 있습니다. 이 문서는 **Claude Code로 작업할 때만 추가로 적용되는 사항**만 담습니다. `AGENTS.md`와 이 문서가 충돌하면 더 구체적인 이 문서를 따르되, 발견 즉시 둘 중 하나를 수정해 일관성을 맞추십시오.

## 1. 이 리포지토리에서 Claude Code를 쓸 때

- 큰 작업(새 Phase 착수, 여러 파일에 걸친 구현)을 시작하기 전에는 **Plan 모드**로 먼저 계획을 세우고, 계획이 `Docs/phases/PhaseN-*.md`의 해당 작업 정의와 일치하는지 확인한 뒤 실행하십시오. 이미 Phase 문서에 상세 작업 지시가 있으므로, Plan 모드에서 새로 설계하기보다는 **Phase 문서를 계획의 근거로 삼는 것**이 원칙입니다.
- 하나의 세션에서 여러 작업 ID(P#-##)를 동시에 진행해야 한다면, 서로 "소유 파일"이 겹치지 않는 작업들만 병렬로 진행하십시오(Agent 도구로 서브에이전트를 병렬 실행하는 것도 가능하나, 사용자가 명시적으로 요청한 경우에만 사용합니다 — 불필요한 서브에이전트 생성은 피하십시오).
- 리서치가 필요한 경우(예: 특정 라이브러리 API 확인, VivoStudio/VivoAcademy 소스 재확인) `fork` 서브에이전트를 활용해 원본 컨텍스트를 오염시키지 않는 것을 권장합니다.

## 2. 이 리포지토리의 `.agents/`, `.claude/` 폴더에 대한 주의

리포지토리 루트의 `.agents/skills/`와 `.claude/skills/`에는 `caveman`, `grill-me`, `llm-wiki`, `ponytail`, `vercel-react-best-practices` 등의 폴더가 이미 존재합니다. 이것들은 **Claude Code 자체의 전역 스킬이 이 프로젝트 디렉터리로 미러링된 것**이며, Fortress 앱이 런타임에 읽는 사용자 워크스페이스용 `.agents/skills/`(`Docs/Architecture.md` §6에서 정의하는 앱 기능)와는 **완전히 무관**합니다.

- 이 폴더들을 Fortress 앱의 스킬 로더 구현(Phase 3) 대상으로 착각하지 마십시오. Phase 3의 스킬 스캐너는 **Fortress 앱이 실행될 때 사용자가 지정한 별도의 워크스페이스 폴더**를 스캔하는 것이지, 이 리포지토리 자체의 `.agents/skills/`를 스캔하는 것이 아닙니다.
- 이 폴더들의 내용을 수정/삭제하지 마십시오(Claude Code 도구 설정의 일부입니다).

## 3. 문서 갱신 습관

- `Docs/TODO.md`를 세션당 최소 한 번 이상 확인/갱신하는 것을 습관화하십시오. Claude Code 세션이 끊기거나 압축(compact)되더라도, `Docs/TODO.md`의 상태가 최신이면 다음 세션(다른 에이전트 포함)이 정확히 이어받을 수 있습니다.
- 작업 중 설계 결정을 바꾸게 되면(예: 특정 라이브러리를 계획과 다른 것으로 교체) 반드시 `Docs/Architecture.md` 또는 해당 `Docs/phases/PhaseN-*.md`를 함께 갱신하십시오. 코드와 문서가 어긋난 상태로 두지 않습니다.

## 4. 언어

이 프로젝트의 기획/설계 문서는 한국어로 작성되어 있습니다(코드 식별자·파일명·기술 용어는 영어). 사용자와의 대화도 한국어를 기본으로 하되, 코드/커밋 메시지/파일 경로는 영어 관례를 따릅니다.
