import type { Dict } from '../ko';

export const evalLockEn: Dict = {
  'eval.lock.chatBlocked': 'Cannot send chats while an evaluation is running.',
  'eval.lock.inputPlaceholder': 'Evaluation in progress. You can type again when it finishes.',
  'eval.lock.banner': 'Evaluation running: {runName} — chats will be available when it finishes.',
  'eval.lock.viewProgress': 'View progress',
};
